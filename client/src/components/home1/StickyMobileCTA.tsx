/* =============================================================================
   Sticky Mobile CTA Bar — DISABLED
   Previously showed fixed bottom bar on mobile with "Call Now" and "Schedule"
   pills. Removed per client request.
   ============================================================================= */

export default function StickyMobileCTA({ onConsultClick: _onConsultClick }: { onConsultClick: () => void }) {
  return null;
}
